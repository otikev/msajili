<% provide(:title, 'Profile') %>
<%= 
	if @current_user.role == Role.applicant
		render 'profile/cv',:user => @current_user,:show_edit => true
	elsif @current_user.role == Role.recruiter
		#render something
	elsif @current_user.role == Role.admin
		#render something
 	end
%>
